---

- hosts: all
  become: true
  tasks:

  - name: update repository index
    dnf:
      update_cache: true

  - name: remove httpd package
    dnf:
      name: httpd
      state: absent

  - name: remove php package
    dnf:
      name: php
      state: absent
